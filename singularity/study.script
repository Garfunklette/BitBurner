var hackingLevel = getHackingLevel();
var studying = FALSE;

if(isBusy() === TRUE) {
  stopAction();
}

while(hackingLevel < 53) {

  if(studying === FALSE) {
    studying = universityCourse("Rothman University", "Study Computer Science");
  }

  sleep(300000);

  hackingLevel = getHackingLevel();

  print("Studying? "+studying);
}
